const { sequelize } = require("../config/db");const createTransaction = async (data) => {  const [result] = await sequelize.query("INSERT INTO transactions (order_id, transaction_code, payment_method, amount, currency, status, paid_at) VALUES (?, ?, ?, ?, ?, ?, ?)", { replacements: [data.order_id, data.transaction_code, data.payment_method, data.amount, data.currency || "VND", data.status || "pending", data.paid_at || null] });  return result;};module.exports = { createTransaction };